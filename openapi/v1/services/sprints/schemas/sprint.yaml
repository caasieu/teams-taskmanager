type: object

required:
  - id
  - name
  - status
  - teamId

properties:

  id:
    type: string
    format: uuid

  name:
    type: string

  status:
    type: string
    enum:
      - PLANNED
      - ACTIVE
      - COMPLETED

  teamId:
    type: string
    format: uuid