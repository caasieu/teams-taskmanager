put:
  summary: Update task status

  operationId: updateTaskStatus

  security:
    - bearerAuth: []

  tags:
    - Tasks

  parameters:

    - name: taskId
      in: path
      required: true
      schema:
        type: string
        format: uuid

  requestBody:
    required: true

    content:
      application/json:
        schema:
          $ref: "../schemas/update-task-status-request.yaml"

  responses:

    "200":
      description: Task status updated

      content:
        application/json:
          schema:
            $ref: "../schemas/task.yaml"

    "400":
      $ref: "../../../components/responses/bad-request.yaml"

    "404":
      $ref: "../../../components/responses/not-found.yaml"